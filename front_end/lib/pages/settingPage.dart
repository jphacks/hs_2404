import 'package:flutter/material.dart';
import 'basePage.dart';

class SettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BasePage(
      body: Center(
        child: Text('いろいろ設定できるようになると思う'),
      ),
    );
  }
}